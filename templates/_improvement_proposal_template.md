# 振り返り（メタ改善）レポート — <案件名>

- 実施: YYYY-MM-DD HH:MM
- 実施者: retrospective
- 対象案件: <docs/ のルート or 案件名>
- 対象プラグインバージョン: vX.Y.Z（この案件を回したときのバージョン）
- データ欠損: <参照できなかった状態ファイルがあれば列挙。なければ「なし」>

## 1. 評価指標サマリ（機械集計）

| 指標 | 値 | 備考 |
|---|---|---|
| 差し戻し回数（フェーズ別） | P0:n P1:n P2:n P3:n P4:n P5:n P6:n | phase_reviews の FAIL 出現 |
| BLOCKER / MAJOR / MINOR / INFO | n / n / n / n | 全レビュー合算 |
| 支配的な指摘カテゴリ | <例: 認可・トレーサビリティ> | 件数の多い順 |
| トレーサビリティ充足率 | n%（仕様ID m 件中 k 件に @spec） | code-review より |
| spec_gaps 件数（起因フェーズ） | n（P2:n P4:n …） | 設計の穴 |
| CR 件数（起因フェーズ） | n（P1:n P2:n …） | 設計確定後の変更 |
| インシデント件数 | n | 実装フェーズの事故 |

### カテゴリ別 指摘件数

| カテゴリ | 件数 | 主な出どころ |
|---|---|---|
| 認可 | n | |
| PII / セキュリティ | n | |
| トレーサビリティ | n | |
| 整合性（画面⇔機能⇔API⇔DDL） | n | |
| 命名 / 型 / 保守性 | n | |
| テスト品質 | n | |
| 非機能 | n | |
| その他 | n | |

## 2. 再発パターン（過去案件との突き合わせ）

> `improvements/` の履歴と今回を突き合わせ、複数案件で繰り返すものを上位に。

| # | パターン（繰り返し出ている指摘） | 出現案件数 | 今回件数 | 構造的弱点か |
|---|---|---|---|---|
| 1 | <例: 認可が「画面の出し分け」頼みで API 認可が設計に書かれない> | 3 | 4 | はい |

## 3. 改善提案

> 各提案は「宛先アーティファクト1つ」「diff レベルの変更」「狙う指標」「期待効果と測り方」「優先度」をセットで。

### P-1 [優先度: 高/中/低] <一行サマリ>

- **宛先**: `references/SPEC_RULES.md` / `agents/<name>.md` / `templates/<name>` / `hooks/<name>` のいずれか1つ
- **根本原因**: <この指摘が繰り返されるのは、宛先の何が欠けている／曖昧／過剰だからか>
- **変更内容（diff レベル）**:
  - 追加 / 修正 / 削除・統合: <具体的な文面・チェック項目・条件>
- **狙う指標**: <カテゴリ別件数のどれを下げるか>
- **期待効果と測り方**: 適用後、次案件で <指標X> が <どうなれば> 成功とみなす
- **副作用 / リスク**: <ルール肥大化・誤検出増など。なければ「小」>

### P-2 [優先度: …] <一行サマリ>
- （同上の形式）

## 4. 案件固有事情として除外したもの

> プラグインの欠陥ではなく、この案件特有の事情。ルール化しても次案件に効かないため提案にしない。

- <例: ユーザー回答の遅延、特殊な業界要件 など>

## 5. 過去提案の答え合わせ（2回目以降）

| 過去提案ID | 適用バージョン | 狙った指標 | 今回の実績 | 判定 |
|---|---|---|---|---|
| P-1（v0.10.0） | v0.10.0 | 認可指摘 件数減 | 4→1 | 効いた（定着） |
| | | | | 効かない（ロールバック候補） |

## 6. 人間が次に取るアクション

1. 上記提案を一件ずつ承認/却下する。
2. 承認分を別セッションでプラグイン作業フォルダに反映し、`improvements/` に採否・適用日・version を記録してコミットする。
3. 次案件完了時に retrospective を再実行し、本レポートの「期待効果」を答え合わせする。
